# CaptionFlow TODO

## LLM 纠错与术语沉淀

- [x] 让 LLM 精修请求同时携带英文原文和本地中文初译，以便纠正本地译文而非只重新翻译。
- [x] 在字幕中区分本地初译、LLM 精修中和 LLM 精修完成状态，并保留精修失败时的本地译文。
- [x] 为 LLM 精修加入可解释的术语候选：显示候选来源及示例，不能自动写入长期词库。
- [x] 提供用户确认、编辑、删除的本地术语表；确认后的术语可作为后续翻译提示的一部分。
- [x] 查明 LLM 精修间歇性失败的原因：2026-09-26 用 deepseek-v4-flash（Anthropic 格式）验收时，第一次 11 句中 4 句失败，请求均返回 HTTP 200；第二次 8 句全部成功。失败原因现已写入系统日志（subsystem `com.taihongteng.CaptionFlow`），第三次（同日）10 句中 5 句失败，日志均为 `stop_reason=max_tokens blocks=[thinking]`：思考内容占满 1024 的 `max_tokens`，没有输出译文。已修复：Anthropic 请求带 `thinking.type = disabled`（`testAnthropicRequestDisablesThinking`）。

- [x] 词库一直是空的（glossary.json 为 0 条）：术语提取入口只在字幕历史的右键菜单里，从未运行过；提取提示词只要专有名词和术语，不要短语；精修只附带原文中原样出现的词条，识别听错的人名（如 "Looks rest"）匹配不上。计划：
  - [x] 在“自定义词汇”里加“从字幕历史分析”按钮，分析最近若干场会话，结果进入候选列表。
  - [x] 提取范围扩大到反复出现的短语和固定搭配，候选旁显示由代码统计的出现次数，仍逐条确认后才写入词库。
  - [x] 把词库中的专有名词（限制数量）作为“可能出现的名字”发给精修请求，让模型把听错的名字还原。

## 语音识别

- [x] 静音时不输出字幕：样本播放结束后，Whisper 每隔几秒把静音识别成 “you” 并译成“你”，既干扰显示，又每条都发出一次 LLM 请求。
- [x] 按语句边界切分识别结果：当前约每 3 秒切一段，会把单词和句子截断（如 “mock-” / “up”，“Microsoft Build” 被切成 “Microsoft.” 和 “built in Seattle”，后者译成“西雅图制造”），还出现过整段丢失（“will speak at Microsoft Build in Seattle”）。已改为优先在 0.5 秒以上的停顿处切开、窗口开头的静音不计入长度（6 秒内没有长停顿时退而在最长的停顿处切）；在 TTS 样本上窗口与句子一一对应。真人语音验证暂时不做（2026-09-26）。

## 本地翻译可用性

- [x] 应用启动时检查英语到简体中文的本地翻译资源是否可用。
- [x] 资源缺失时，在开始字幕前显示明确提示和下载/安装入口，而不是静默回退或失败。
- [x] 安装完成后重新检查资源状态，并允许用户开始本地翻译。
- [x] 在无网络、安装失败和资源不可用时保留英文字幕并显示可操作错误信息。
- [x] 为资源就绪、缺失和安装失败状态增加可重复的自动化测试。

## 验收

2026-09-26 用当前选中的 deepseek-v4-flash 跑过 `LiveLLMAcceptanceTests`（CI 不创建 `/tmp/captionflow-live-llm`，因此会跳过）。

- [x] 使用包含专有名词的英语样本，确认 LLM 能修正本地初译且不会阻断后续字幕。本地初译“看起来休息会在西雅图制造”被精修为“卢卡斯·雷斯特将在西雅图微软 Build 大会上发表演讲”；下一句在上一句精修结束前已进入字幕，会话保持运行。
- [x] 用户确认术语后，使用后续样本确认术语会被稳定采用；未确认候选不得自动持久化。模型提出的 Kubernetes、sprint、backlog 只进候选列表，glossary.json 仍为空。确认 “minutes=会议纪要” 后，后续两句分别精修为“请把会议纪要发给团队”和“我在会前看过了会议纪要”，不再使用“分钟”。

## 设置

- [x] 「关于」还是占位页，只显示“即将推出”，没有版本、说明或其他内容。现为版本号、一句应用说明，以及项目主页链接。
